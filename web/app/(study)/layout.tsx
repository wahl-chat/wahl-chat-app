/**
 * Study route group layout
 * Minimal layout that inherits from root layout providers
 */
export default function StudyGroupLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
