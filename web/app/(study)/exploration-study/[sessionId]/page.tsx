/**
 * This page serves as a redirect entry point.
 * The layout handles loading the session and redirecting
 * to the appropriate step based on the session state.
 */
export default function StudySessionPage() {
  // The layout will redirect to the correct page based on session state
  return null;
}
